title: Refine discovered capabilities
status: planned
summary: Provide an iterative review workflow that improves generated capability
  files after creation.
intent: Help teams turn agent-generated drafts into maintained capability specs
  by reviewing evidence, gaps, naming, and acceptance criteria over time.
acceptance:
  - Reports generated capabilities that still need human review.
  - Reuses assessment and review commands to compare each generated capability
    against implementation references.
  - Prioritizes refinement of capabilities with weak code evidence, shallow
    inspected path summaries, or acceptance criteria inferred from documentation
    instead of implementation behavior.
  - Suggests specific edits for vague acceptance criteria, weak references,
    missing verification, and poor area placement.
  - Lets users accept, revise, or remove generated draft capabilities without
    losing the audit trail of why they were proposed.
  - Updates status and review evidence only through explicit user or policy
    action.
guidance:
  - Treat generated capability files as the start of a maintenance loop, not a
    one-time import.
  - Ask agents to revisit code paths when the draft capability lacks concrete
    implementation evidence.
  - Keep recommendations grouped by the smallest useful edit.
  - Do not automatically promote generated capabilities to implemented or
    verified status from agent prose alone.
#
#
agent:
  # -----------------------------
  # machine managed agent metadata
  # refresh all review evidence: capabilitykit sync-review
  # refresh this review evidence: capabilitykit sync-review core/discovery/refine-discovered-capabilities
  # review this capability: capabilitykit assess core/discovery/refine-discovered-capabilities
  # ask an agent to review this capability: capabilitykit agent-review core/discovery/refine-discovered-capabilities --command codex --handoff stdin
  # save agent review output: capabilitykit review-result core/discovery/refine-discovered-capabilities --input review.json --save
  # -----------------------------
  inputs:
    - generated capability files
    - implementation references
    - inspected code path summaries
    - assessment output
    - agent review output
  outputs:
    - capability refinement recommendations
    - accepted review evidence
    - updated capability statuses when explicitly requested
  depends_on:
    - core/discovery/generate-draft-capability-files
    - core/assessment/assess-implementation-coverage
    - core/agents/review-capability-with-agent
    - core/agents/ingest-agent-review-result
  verification:
    automated:
      - id: discovery-refinement-tests
        description: Unit tests verify generated capabilities are surfaced with
          actionable refinement recommendations.
        command: npm test -- packages/core/tests/discoveryRefinement.test.ts
      - id: cli-build
        description: TypeScript build verifies refinement command integration.
        command: npm run build
    manual:
      - Run refinement against generated draft capabilities and confirm the
        recommendations help a maintainer improve the map without rereading the
        whole project.
    gaps:
      - Status promotion policy should remain configurable before any automated
        update workflow is enabled.
    ignore_gaps: []
  review:
    depth: partial
    gaps:
      - Reports generated capabilities that still need human review. Add
        agent.implementation.references to
        core/discovery/refine-discovered-capabilities, or lower the status if
        this capability is only planned.
      - Reuses assessment and review commands to compare each generated
        capability against implementation references. Add
        agent.implementation.references to
        core/discovery/refine-discovered-capabilities, or lower the status if
        this capability is only planned.
      - Prioritizes refinement of capabilities with weak code evidence, shallow
        inspected path summaries, or acceptance criteria inferred from
        documentation instead of implementation behavior. Add
        agent.implementation.references to
        core/discovery/refine-discovered-capabilities, or lower the status if
        this capability is only planned.
      - Suggests specific edits for vague acceptance criteria, weak references,
        missing verification, and poor area placement. Add
        agent.implementation.references to
        core/discovery/refine-discovered-capabilities, or lower the status if
        this capability is only planned.
      - Lets users accept, revise, or remove generated draft capabilities
        without losing the audit trail of why they were proposed. Add
        agent.implementation.references to
        core/discovery/refine-discovered-capabilities, or lower the status if
        this capability is only planned.
      - Updates status and review evidence only through explicit user or policy
        action. Add agent.implementation.references to
        core/discovery/refine-discovered-capabilities, or lower the status if
        this capability is only planned.
    evidence: []
    intent_summary: Implementation evidence synchronized from deterministic
      assessment for core/discovery/refine-discovered-capabilities.
    criteria:
      - criterion: Reports generated capabilities that still need human review.
        status: uncovered
        evidence: []
        notes: The capability has no agent.implementation.references, so the assessor
          has no concrete files to inspect. Add agent.implementation.references
          to core/discovery/refine-discovered-capabilities, or lower the status
          if this capability is only planned.
      - criterion: Reuses assessment and review commands to compare each generated
          capability against implementation references.
        status: uncovered
        evidence: []
        notes: The capability has no agent.implementation.references, so the assessor
          has no concrete files to inspect. Add agent.implementation.references
          to core/discovery/refine-discovered-capabilities, or lower the status
          if this capability is only planned.
      - criterion: Prioritizes refinement of capabilities with weak code evidence,
          shallow inspected path summaries, or acceptance criteria inferred from
          documentation instead of implementation behavior.
        status: uncovered
        evidence: []
        notes: The capability has no agent.implementation.references, so the assessor
          has no concrete files to inspect. Add agent.implementation.references
          to core/discovery/refine-discovered-capabilities, or lower the status
          if this capability is only planned.
      - criterion: Suggests specific edits for vague acceptance criteria, weak
          references, missing verification, and poor area placement.
        status: uncovered
        evidence: []
        notes: The capability has no agent.implementation.references, so the assessor
          has no concrete files to inspect. Add agent.implementation.references
          to core/discovery/refine-discovered-capabilities, or lower the status
          if this capability is only planned.
      - criterion: Lets users accept, revise, or remove generated draft capabilities
          without losing the audit trail of why they were proposed.
        status: uncovered
        evidence: []
        notes: The capability has no agent.implementation.references, so the assessor
          has no concrete files to inspect. Add agent.implementation.references
          to core/discovery/refine-discovered-capabilities, or lower the status
          if this capability is only planned.
      - criterion: Updates status and review evidence only through explicit user or
          policy action.
        status: uncovered
        evidence: []
        notes: The capability has no agent.implementation.references, so the assessor
          has no concrete files to inspect. Add agent.implementation.references
          to core/discovery/refine-discovered-capabilities, or lower the status
          if this capability is only planned.
    ignore_findings: []
    done: false
