title: Generate draft capability files
status: implemented
summary: Turn agent-discovered project capabilities into draft
  `.capability.yaml` files with implementation references and explicit
  verification gaps.
intent: Let a user ask their agent of choice to create a useful first
  `.capabilities/` map for the main functionality of an existing application.
acceptance:
  - Converts an organized generation plan into valid capability files, deriving
    the plan from the durable discovery report when the user does not provide an
    inspectable or edited plan explicitly.
  - Writes files under a neat area and subfolder structure that reflects the
    application's domains and workflows.
  - Populates title, status, summary, intent, acceptance criteria, and
    verification gaps while deriving capability IDs and areas from the selected
    folder paths, and includes implementation references for discovered existing
    behavior.
  - Preserves source-backed acceptance evidence from discovery as draft
    criterion-level review metadata so generated capabilities have an initial
    coverage trail before a later semantic review.
  - Requires every generated capability for discovered existing behavior to
    include concrete implementation references from code inspection, not only
    documentation or package metadata.
  - Uses implemented status for discovered existing behavior with concrete
    implementation references, uses planned only for explicitly retained
    proposals for absent behavior, and never claims generated files are
    verified.
  - Refuses to overwrite existing capability files unless force is requested.
  - Writes files only after the user explicitly requests generation and reports
    path collisions before applying changes.
  - Preserves a durable discovery report with the report ID, evidence,
    confidence notes, inspected paths, grouping decisions, and rejected or
    quarantined candidates.
guidance:
  - Generated capability files should be useful to navigate even before they are
    perfect.
  - Keep explicit plan generation available for users who want to inspect or
    edit structure before generation, without requiring that step for the
    default workflow.
  - Prefer capability IDs that describe product behavior rather than internal
    module names.
  - Preserve evidence paths so maintainers can quickly review why each
    capability was proposed.
  - Treat generated review metadata as a starting audit trail, not proof that a
    capability is verified.
  - Reject or quarantine candidates that lack enough code evidence to be useful
    as draft capabilities.
planning:
  story_map:
    backbone: Existing project discovery
    step: Generate capability files
    release: reverse-engineering
    order: 40
#
#
agent:
  # -----------------------------
  # machine managed agent metadata
  # review all capabilities and save evidence: capabilitykit review
  # review this capability and save evidence: capabilitykit review core/discovery/generate-draft-capability-files
  # run deterministic review only: capabilitykit review core/discovery/generate-draft-capability-files --deterministic-only
  # ask an agent and save review evidence: capabilitykit review core/discovery/generate-draft-capability-files --agent codex --arg exec --handoff stdin
  # validate saved agent output without writing: capabilitykit review-result core/discovery/generate-draft-capability-files --input review.json
  # -----------------------------
  inputs:
    - capability discovery report
    - selected organized generation plan
    - existing .capabilities folder
  outputs:
    - .capabilities/**/*.capability.yaml
    - generation summary
    - durable discovery report
  depends_on:
    - core/discovery/organize-discovered-capability-map
    - core/validation/validate-capability-files
    - core/validation/verify-implementation-references
  implementation:
    references:
      - packages/core/src/discoveryGeneration.ts
      - packages/core/src/discoveryOrganization.ts
      - packages/core/src/discoveryReport.ts
      - packages/core/src/index.ts
      - packages/cli/src/index.ts
      - packages/core/tests/discoveryGeneration.test.ts
  verification:
    automated:
      - id: generated-capability-validation-tests
        description: Unit tests verify generated draft files parse and validate.
        command: npm test -- packages/core/tests/discoveryGeneration.test.ts
      - id: cli-build
        description: TypeScript build verifies generation APIs and CLI options.
        command: npm run build
    manual:
      - Generate draft capabilities in a fixture project and confirm the
        resulting folder structure is understandable to a new maintainer.
    gaps:
      - Generated acceptance criteria still need review because source evidence
        cannot fully explain product intent.
  review:
    depth: tested
    source: coding-agent
    gaps:
      - Generated acceptance criteria still require human review because source
        evidence cannot fully establish product intent.
      - The declared manual readability check for a generated fixture map was
        not performed.
      - The optional CLI --plan omission path is source-reviewed and
        build-checked but does not have a dedicated CLI integration test.
    evidence:
      - "npm test -- packages/core/tests/discoveryGeneration.test.ts: passed, 7
        tests"
      - "npm test -- packages/core/tests/discovery.test.ts
        packages/core/tests/discoveryOrganization.test.ts
        packages/core/tests/discoveryGeneration.test.ts: passed, 14 tests"
      - "npm run build: passed for @capabilitykit/core and @capabilitykit/cli"
    intent_summary: Generate a useful first-pass .capabilities map from durable
      discovery evidence. Drafts use domain-oriented paths, preserve code
      references and verification gaps, derive a plan automatically unless an
      edited plan is supplied, and write only after explicit application.
    criteria:
      - criterion: Converts an organized generation plan into valid capability files,
          deriving the plan from the durable discovery report when the user does
          not provide an inspectable or edited plan explicitly.
        status: covered
        evidence:
          - packages/cli/src/index.ts:2594
          - packages/core/src/discoveryGeneration.ts:131
          - packages/core/src/discoveryGeneration.ts:153
          - packages/core/tests/discoveryGeneration.test.ts:59
        notes: The CLI accepts an optional plan and derives one from the durable report
          when omitted. Generated YAML is parsed before writing.
      - criterion: Writes files under a neat area and subfolder structure that reflects
          the application's domains and workflows.
        status: covered
        evidence:
          - packages/core/src/discoveryOrganization.ts:159
          - packages/core/src/discoveryOrganization.ts:219
          - packages/core/src/discoveryOrganization.ts:258
          - packages/core/tests/discoveryOrganization.test.ts:42
        notes: Area hints become stable paths with up to two segments. Ambiguous
          structural hints are flagged for review.
      - criterion: Populates title, status, summary, intent, acceptance criteria, and
          verification gaps while deriving capability IDs and areas from the
          selected folder paths, and includes implementation references for
          discovered existing behavior.
        status: covered
        evidence:
          - packages/core/src/discoveryGeneration.ts:89
          - packages/core/src/discoveryGeneration.ts:146
          - packages/core/src/parseCapability.ts:44
          - packages/core/tests/discoveryGeneration.test.ts:70
        notes: Draft fields and references are populated from the candidate. IDs and
          areas are path-derived.
      - criterion: Preserves source-backed acceptance evidence from discovery as draft
          criterion-level review metadata so generated capabilities have an
          initial coverage trail before a later semantic review.
        status: covered
        evidence:
          - packages/core/src/discoveryGeneration.ts:34
          - packages/core/src/discoveryGeneration.ts:47
          - packages/core/src/discoveryGeneration.ts:119
          - packages/core/tests/discoveryGeneration.test.ts:76
        notes: Generated capabilities include agent.review criteria populated from
          acceptance_evidence and marked done false so later review can refine
          them.
      - criterion: Requires every generated capability for discovered existing behavior
          to include concrete implementation references from code inspection,
          not only documentation or package metadata.
        status: covered
        evidence:
          - packages/core/src/discoveryReport.ts:241
          - packages/core/src/discoveryReport.ts:273
          - packages/core/src/discoveryGeneration.ts:126
          - packages/core/tests/discoveryGeneration.test.ts:96
          - packages/core/tests/discoveryGeneration.test.ts:129
        notes: Generation revalidates durable reports. Documentation-only and missing
          evidence paths are rejected.
      - criterion: Uses implemented status for discovered existing behavior with
          concrete implementation references, uses planned only for explicitly
          retained proposals for absent behavior, and never claims generated
          files are verified.
        status: covered
        evidence:
          - packages/core/src/discoveryOrganization.ts:127
          - packages/core/src/discoveryOrganization.ts:266
          - packages/core/tests/discoveryGeneration.test.ts:73
          - packages/core/tests/discoveryGeneration.test.ts:156
        notes: The plan schema only allows implemented for discovered candidates and
          planned for retained proposals.
      - criterion: Refuses to overwrite existing capability files unless force is
          requested.
        status: covered
        evidence:
          - packages/core/src/discoveryGeneration.ts:159
          - packages/core/src/discoveryGeneration.ts:177
          - packages/core/tests/discoveryGeneration.test.ts:82
        notes: Existing files become collisions and block writes unless force is set.
      - criterion: Writes files only after the user explicitly requests generation and
          reports path collisions before applying changes.
        status: covered
        evidence:
          - packages/core/src/discoveryGeneration.ts:159
          - packages/core/src/discoveryGeneration.ts:180
          - packages/core/src/discoveryGeneration.ts:197
          - packages/cli/src/index.ts:2589
          - packages/cli/src/index.ts:2614
          - packages/core/tests/discoveryGeneration.test.ts:59
        notes: Preview is the default. Collision detection runs before writes, and
          non-forced collisions prevent application.
      - criterion: Preserves a durable discovery report with the report ID, evidence,
          confidence notes, inspected paths, grouping decisions, and rejected or
          quarantined candidates.
        status: covered
        evidence:
          - packages/core/src/discoveryReport.ts:8
          - packages/core/src/discoveryReport.ts:35
          - packages/core/src/discoveryReport.ts:338
          - packages/core/src/discoveryGeneration.ts:191
          - packages/core/src/discoveryGeneration.ts:193
          - packages/core/src/discoveryOrganization.ts:313
          - packages/core/tests/discovery.test.ts:110
          - packages/core/tests/discoveryGeneration.test.ts:76
        notes: The durable report preserves evidence and provenance. The generation
          audit preserves the selected plan and grouping decisions.
    done: true
