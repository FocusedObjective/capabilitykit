title: Organize discovered capability map
status: implemented
summary: Create a readable proposed folder and dependency structure from
  agent-discovered candidates before capability files are written.
intent: Make the generated `.capabilities/` tree feel like a product map rather
  than a dump of source-code observations.
acceptance:
  - Groups agent-discovered candidates into meaningful area and subarea folders
    before capability files are written.
  - Suggests capability IDs and filenames that are stable, readable, and aligned
    with the generated folder structure.
  - Detects likely dependencies between discovered capabilities when code
    evidence or workflow relationships support them.
  - Provides a summary index of generated areas and top-level capabilities.
  - Flags ambiguous grouping decisions for human review instead of hiding them.
  - Produces an inspectable generation plan that records grouping decisions,
    proposed paths, collisions, and dependency suggestions without moving or
    overwriting files.
guidance:
  - Prefer product domains, workflows, and user-visible features over framework
    folders when naming areas.
  - Preserve the agent's inspected code evidence when grouping candidates.
  - Keep dependency inference conservative.
  - Avoid creating deep folder trees unless the project has enough behavior to
    justify them.
planning:
  story_map:
    backbone: Existing project discovery
    step: Organize capability map
    release: reverse-engineering
    order: 30
#
#
agent:
  # -----------------------------
  # machine managed agent metadata
  # review all capabilities and save evidence: capabilitykit review
  # review this capability and save evidence: capabilitykit review core/discovery/organize-discovered-capability-map
  # run deterministic review only: capabilitykit review core/discovery/organize-discovered-capability-map --deterministic-only
  # ask an agent and save review evidence: capabilitykit review core/discovery/organize-discovered-capability-map --agent codex --arg exec --handoff stdin
  # validate saved agent output without writing: capabilitykit review-result core/discovery/organize-discovered-capability-map --input review.json
  # -----------------------------
  inputs:
    - discovery candidates
    - inspected code path summary
    - existing capability IDs
  outputs:
    - organized capability generation plan
    - dependency suggestions
    - generated capability index summary
  depends_on:
    - core/discovery/discover-project-capability-candidates
    - core/graph/analyze-capability-impact
  implementation:
    references:
      - packages/core/src/discoveryOrganization.ts
      - packages/core/src/index.ts
      - packages/core/tests/discoveryOrganization.test.ts
  verification:
    automated:
      - id: capability-map-organization-tests
        description: Unit tests verify stable ID, folder, and dependency suggestions for
          fixture projects.
        command: npm test -- packages/core/tests/discoveryOrganization.test.ts
      - id: generated-map-compiles
        description: Generated capability maps compile after organization.
        command: npm run build
    manual:
      - Review generated folders for a sample app and confirm a maintainer can
        answer what the app does without reading source code first.
    gaps:
      - Dependency inference may need project-specific review for cross-cutting
        infrastructure and shared services.
  review:
    depth: partial
    source: coding-agent
    gaps:
      - Grouping quality depends on upstream likely_area hints; inspected code
        path summaries are not used to derive or improve product-domain
        grouping.
      - Dependency inference uses likely_relationships text matching only and
        does not infer relationships from code evidence.
      - The requested manual sample-app readability review was not performed.
    evidence:
      - "npm test -- packages/core/tests/discoveryOrganization.test.ts: passed,
        2 tests"
      - "npm test -- packages/core/tests/discoveryGeneration.test.ts: passed, 7
        tests"
      - "npm run build: passed for @capabilitykit/core and @capabilitykit/cli"
    intent_summary: Create an inspectable, non-destructive plan that turns
      discovered candidates into a readable capability tree, proposes stable IDs
      and paths, summarizes areas, surfaces ambiguity, and conservatively
      suggests dependencies before files are generated.
    criteria:
      - criterion: Groups agent-discovered candidates into meaningful area and subarea
          folders before capability files are written.
        status: partial
        evidence:
          - packages/core/src/discoveryOrganization.ts:159
          - packages/core/src/discoveryOrganization.ts:219
          - packages/core/src/discoveryOrganization.ts:258
          - packages/cli/src/index.ts:2562
          - packages/core/tests/discoveryOrganization.test.ts:51
        notes: The plan derives at most two folder segments from likely_area before
          generation. Product-domain quality depends on the upstream hint;
          inspected code paths are not used to improve grouping.
      - criterion: Suggests capability IDs and filenames that are stable, readable, and
          aligned with the generated folder structure.
        status: covered
        evidence:
          - packages/core/src/discoveryOrganization.ts:149
          - packages/core/src/discoveryOrganization.ts:220
          - packages/core/src/discoveryOrganization.ts:253
          - packages/core/src/discoveryOrganization.ts:258
          - packages/core/tests/discoveryOrganization.test.ts:51
        notes: Deterministic slugging is applied consistently to IDs, filenames, and
          proposed paths.
      - criterion: Detects likely dependencies between discovered capabilities when code
          evidence or workflow relationships support them.
        status: partial
        evidence:
          - packages/core/src/discoveryOrganization.ts:177
          - packages/core/src/discoveryOrganization.ts:182
          - packages/core/src/discoveryOrganization.ts:247
          - packages/core/tests/discoveryOrganization.test.ts:46
          - packages/core/tests/discoveryOrganization.test.ts:56
        notes: Conservative suggestions are generated from likely_relationships text and
          ambiguous matches are flagged. The organizer does not infer
          dependencies from inspected code evidence.
      - criterion: Provides a summary index of generated areas and top-level capabilities.
        status: covered
        evidence:
          - packages/core/src/discoveryOrganization.ts:52
          - packages/core/src/discoveryOrganization.ts:295
          - packages/core/src/discoveryOrganization.ts:301
          - packages/core/tests/discoveryOrganization.test.ts:57
        notes: The returned plan contains sorted area indexes, all capability IDs, and
          top-level capability IDs.
      - criterion: Flags ambiguous grouping decisions for human review instead of hiding
          them.
        status: covered
        evidence:
          - packages/core/src/discoveryOrganization.ts:167
          - packages/core/src/discoveryOrganization.ts:170
          - packages/core/src/discoveryOrganization.ts:221
          - packages/core/src/discoveryOrganization.ts:224
          - packages/core/tests/discoveryOrganization.test.ts:66
        notes: Deep area hints, source-like area names, and non-high-confidence
          candidates produce review flags.
      - criterion: Produces an inspectable generation plan that records grouping
          decisions, proposed paths, collisions, and dependency suggestions
          without moving or overwriting files.
        status: covered
        evidence:
          - packages/core/src/discoveryOrganization.ts:37
          - packages/core/src/discoveryOrganization.ts:58
          - packages/core/src/discoveryOrganization.ts:269
          - packages/cli/src/index.ts:2562
          - packages/core/src/discoveryGeneration.ts:180
          - packages/core/tests/discoveryGeneration.test.ts:59
        notes: The organizer returns a plan object without filesystem operations. The
          CLI exposes it separately, and generation previews without writing
          unless apply is explicit.
    done: false
