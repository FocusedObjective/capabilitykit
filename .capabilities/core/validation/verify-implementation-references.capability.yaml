title: Verify implementation references
status: implemented
summary: Confirm implemented and verified capabilities point to
  agent.implementation.references values that exist in the repository.
intent: Help maintainers and agents trust that completed capabilities are tied
  to inspectable code or documentation.
acceptance:
  - Checks agent.implementation.references for capabilities marked implemented
    or verified.
  - Reports a verification gap when a referenced implementation path does not
    exist.
  - Does not report a gap for agent.implementation.references values that
    resolve under the project root.
guidance:
  - Treat missing reference targets as verification gaps so validation remains
    advisory by default.
  - Do not require planned capabilities to have agent.implementation.references.
planning:
  story_map:
    backbone: Capability authoring
    step: Check capability evidence
    release: mvp
    order: 30
#
#
agent:
  # -----------------------------
  # machine managed agent metadata
  # review all capabilities and save evidence: capabilitykit review
  # review this capability and save evidence: capabilitykit review core/validation/verify-implementation-references
  # run deterministic review only: capabilitykit review core/validation/verify-implementation-references --deterministic-only
  # ask an agent and save review evidence: capabilitykit review core/validation/verify-implementation-references --agent codex --handoff stdin
  # validate saved agent output without writing: capabilitykit review-result core/validation/verify-implementation-references --input review.json
  # -----------------------------
  inputs:
    - .capabilities/**/*.capability.yaml
    - agent.implementation.references values
  outputs:
    - verification gap list
  depends_on:
    - core/validation/validate-capability-files
  implementation:
    references:
      - packages/core/src/validateCapabilities.ts
      - packages/core/tests/validation.test.ts
  verification:
    automated:
      - id: implementation-reference-tests
        description: Unit tests cover existing and missing implementation reference targets.
        command: npm test
      - id: dogfood-validate
        description: The CLI validates CapabilityKit's own capability map and
          agent.implementation.references values.
        command: npm run capabilitykit -- validate
    manual:
      - Break one agent.implementation.references value locally and confirm
        validation reports the missing path.
  review:
    depth: verified
    source: coding-agent
    intent_summary: Coding-agent review confirmed implemented/verified capabilities
      have implementation reference paths checked under the project root,
      missing paths become verification gaps, and existing paths are accepted.
    criteria:
      - criterion: Checks agent.implementation.references for capabilities marked
          implemented or verified.
        status: covered
        evidence:
          - packages/core/src/validateCapabilities.ts:118
          - packages/core/src/validateCapabilities.ts:130
          - packages/core/src/validateCapabilities.ts:131
          - packages/core/tests/validation.test.ts:144
      - criterion: Reports a verification gap when a referenced implementation path does
          not exist.
        status: covered
        evidence:
          - packages/core/src/validateCapabilities.ts:25
          - packages/core/src/validateCapabilities.ts:132
          - packages/core/src/validateCapabilities.ts:136
          - packages/core/tests/validation.test.ts:163
      - criterion: Does not report a gap for agent.implementation.references values that
          resolve under the project root.
        status: covered
        evidence:
          - packages/core/src/validateCapabilities.ts:25
          - packages/core/src/validateCapabilities.ts:132
          - packages/core/tests/validation.test.ts:155
          - packages/core/tests/validation.test.ts:170
    done: true
